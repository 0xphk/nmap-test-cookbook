require 'foodcritic'

desc 'Run Foodcritic lint checks'
FoodCritic::Rake::LintTask.new(:foodcritic) do |t|
  t.options = {
    :fail_tags => ['any'],
    :tags => []
  }
end

desc 'Run test'
task :default => :foodcritic
end